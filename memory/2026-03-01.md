# Memory — Sunday, March 1, 2026

## Daily Threads Report
- Cron ran at 7:00 UTC (10:00 AM Riyadh) — sent 20 open threads to Abu Sattam via Discord DM
- Added **thread owner (👤 Owner)** field to each thread block in `send_report_dm.js`
- Script path: `/root/.openclaw/workspace/send_report_dm.js`

## GitHub
- Created new private repo `moathdev/Tahadou` via GitHub API

## Project: تهادوا تحابوا (Tahadou) — Eid Gift Exchange Platform
Repo: https://github.com/moathdev/Tahadou (private, moathdev)

### What was built today (full session):
- **Real Laravel 11 scaffold** — downloaded skeleton from GitHub, merged with custom code
- **Base Controller.php** added (was missing)
- **Arabic language support** — `lang/ar/app.php`, `lang/ar/validation.php`, RTL layout, `SetLocale` middleware, session-based language switcher (`/lang/ar` ↔ `/lang/en`), default locale = `ar`
- **All views updated** to use `__()` translation helpers
- **Arabic project name**: تهادوا تحابوا | tagline: تبادل هدايا العيد بكل محبة
- **Footer**: Muath Aljohani / moath.co (hardcoded, always fixed)
- **Sticky footer**: body `flex flex-col`, main `flex-1`

### DB Schema:
- `groups`: id, uuid, name, max_participants, max_gift_price (nullable), admin_code (hashed), is_locked, is_drawn
- `participants`: id, group_id, name, phone_number (unique per group), interests (JSON), assigned_to_id (nullable FK)
- Migration added: `2025_01_02_000001_add_max_gift_price_to_groups_table.php`

### Key fixes:
- `Rule::unique()->whereHas()` bug — fixed by resolving group_id first, then `->where('group_id', $groupId)`
- Phone validation: Saudi mobile regex `^(\+?966|0)5[0-9]{8}$`
- Name validation: min 3 chars, Unicode letters only `[\p{L}\s\-\.]+`
- Gift interests moved from config labels to `lang/` translations as `interest_{key}` keys
- `dir="ltr"` on phone input and phone display
- `dir="ltr"` on `🎁 → name` badge (RTL arrow fix)

### Admin Dashboard features:
- Participant list shows: name, phone, interests (badges), draw assignment badge (`🎁 → receiver`)
- After draw: WhatsApp button per participant (`ms-auto` — left in Arabic, right in English)
- WhatsApp button → mark as sent (localStorage per group UUID) + "↺ إعادة إرسال" resend button
- Max gift price banner below stats (amber, shown only if set)
- Draw success flash message translated

### WhatsApp message includes:
- Giver name, group name, receiver name, receiver interests
- Max gift price line (only if set): `⚠️ الحد الأقصى لسعر الهدية: X ريال`

### Removed:
- WhatsApp queue job (`SendWhatsAppNotification`) — replaced by direct links
- Docker Compose (removed per Abu Sattam request)
- Excel/XLSX download (replaced by WhatsApp buttons in dashboard)
- `ext-zip` requirement from composer.json

### README: English only, fully updated

## ReturnPlus KB Weekly Update — 06:00 UTC
- Scraped Salla: ⭐ 5/5 (28 reviews), price 149 SAR/month
- Scraped ZID: ⭐ 4/5 (17 reviews), price ~180.55 SAR/month
- Updated GitHub (moathdev/ObsidianRetrunPlus): ar/reviews.md + en/reviews.md
- Telegram notification sent via @moathdev_bot ✅

## Tahadou Project — Session Summary (15:37–23:59 UTC)
كل العمل اللي صار على مشروع تهادوا اليوم:
- ترجمة نص الحديث النبوي للإنجليزي + إضافته للـ homepage
- ترجمة كل النصوص العربية الـ hardcoded (footer، رسالة واتساب، dashboard، JS)
- إصلاح bug الـ PRG pattern (refresh ينشئ group جديد)
- حذف WhatsApp API config (WHATSAPP_API_URL, WHATSAPP_API_TOKEN)
- تحديث README + إضافة MIT License
- الـ repo صار Public (مفتوح المصدر)
- Branch protection على main (PR + موافقة أبو سطام)
- السماح بتكرار رقم الهاتف في نفس المجموعة (migration جديد)
- حذف Excel export كامل (XlsxExporter.php + route + methods)
- إضافة SEO meta tags (OG + Twitter Card)
- تصحيح git author: كل commits باسم "Muath Aljohani" وإيميل a@moath.co
- **Live:** t.moath.co | **GitHub:** github.com/moathdev/Tahadou
- **git config:** user.name="Muath Aljohani" user.email="a@moath.co"
